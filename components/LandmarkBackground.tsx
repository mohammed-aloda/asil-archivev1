import React from 'react';

const LandmarkBackground: React.FC = () => {
    return (
        <div className="fixed inset-0 pointer-events-none z-0 overflow-hidden opacity-10">
            {/* Background sketches removed per user request */}
        </div>
    );
};

export default LandmarkBackground;
